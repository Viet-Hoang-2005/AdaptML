apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

helmCharts:
  - name: harbor
    repo: https://helm.goharbor.io
    version: 1.15.0
    releaseName: mlops-paas-harbor
    namespace: default
    valuesFile: values.yaml
