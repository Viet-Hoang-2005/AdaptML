apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: harbor-registry-ingressroute
  namespace: harbor
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`registry.mlops-nids-nt114.id.vn`)
      kind: Rule
      services:
        - name: harbor
          port: 80
