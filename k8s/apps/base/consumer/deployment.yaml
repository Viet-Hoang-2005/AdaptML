# consumer-deployment.yaml: Triển khai Kafka Consumer để tiêu thụ dữ liệu từ Redpanda và phát hiện Data Drift với Evidently
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mlops-paas-consumer
  annotations:
    description: "Kafka Consumer to process network traffic data in batches and store predictions"
  labels:
    app: mlops-paas-consumer
spec:
  # Consumer chỉ cần 1 replica (dùng kafka group consumer) là đủ để dọn dẹp
  replicas: 1
  selector:
    matchLabels:
      app: mlops-paas-consumer
  template:
    metadata:
      labels:
        app: mlops-paas-consumer
    spec:
      nodeSelector:
        workload-type: worker
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - mlops-paas-consumer
                topologyKey: "kubernetes.io/hostname"
      initContainers:
        - name: init-kafka-topic
          image: docker.redpanda.com/redpandadata/redpanda:v23.3.21
          command: ["/bin/sh", "-c"]
          args: ["rpk topic create mlops_paas_production_data --brokers redpanda-service:9092 || true"]
      containers:
        - name: consumer
          image: mlops-paas-consumer:latest
          imagePullPolicy: Always
          command: ["python", "-m", "src.main"]
          envFrom:
            - configMapRef:
                name: mlops-paas-consumer-config
          env:
            - name: CONTROL_PLANE_WEBHOOK_SECRET
              valueFrom:
                secretKeyRef:
                  name: mlops-paas-secret
                  key: CONTROL_PLANE_WEBHOOK_SECRET
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: github-secrets
                  key: GITHUB_TOKEN
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-secrets
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-secrets
                  key: DB_PASSWORD
          # Cấu hình tài nguyên cho Consumer
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
